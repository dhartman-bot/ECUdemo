{
  "name": "tractor-ecu",
  "version": "1.0.0",
  "description": "ECU controller for agricultural equipment",
  "main": "src/main.c",
  "scripts": {
    "build": "gcc -o build/ecu_controller src/**/*.c src/*.c -I./src -lm",
    "run": "build/ecu_controller",
    "test": "gcc -o build/test_runner tests/**/*.c src/**/*.c -I./src -lm && build/test_runner"
  },
  "keywords": ["embedded", "ecu", "agricultural", "controller"],
  "author": "Demo",
  "license": "MIT"
}
